# NodeJS API for private chatting app built using ReactJs

This is NodeJS API for private chatting app built using ReactJs. The API is built using Nodejs and MongoDB.


## Installation 
The below commands to install this API.
1. `npm install`
2. `node server.js`
